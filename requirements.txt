osmnx>=1.9
geopandas>=0.14
pandas>=2.2
shapely>=2.0
networkx>=3.2
scikit-learn>=1.4
scipy>=1.11
xgboost>=2.0
mapie>=0.9
folium>=0.16
branca>=0.7
pyarrow>=15.0
openpyxl>=3.1
# CUDA build (RTX 4060 confirmed working 2026-08-31). Falls back to CPU
# automatically at runtime if no GPU is present (see gat_temporal.py's
# _resolve_device) - only the install source differs for a CPU-only
# machine: swap this index-url for https://download.pytorch.org/whl/cpu.
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.5
torch_geometric>=2.6
requests>=2.31
pytest>=8.0
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
